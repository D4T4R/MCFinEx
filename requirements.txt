# Streamlit Community Cloud installs from this file. It mirrors the runtime
# extras in pyproject.toml; the dev extras are deliberately absent.
#
# The project itself comes first: the pages import mcfinex.*, which needs the
# package on the path, not merely present in the checkout.
-e .
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
openpyxl>=3.1
streamlit>=1.40
pandas>=2.0
altair>=5.0
psycopg[binary]>=3.1
